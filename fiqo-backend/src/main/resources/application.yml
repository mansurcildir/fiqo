server:
  port: 8080

logging:
  level:
    root: WARN

fiqo-db:
  host: ${FIQO_DB_HOST:localhost}
  port: ${FIQO_DB_PORT:5432}
  db: ${FIQO_DB_DATABASE:fiqo}
  username: ${FIQO_DB_USERNAME:admin}
  password: ${FIQO_DB_PASSWORD:admin123}
  url: jdbc:postgresql://${fiqo-db.host}:${fiqo-db.port}/${fiqo-db.db}

fiqo:
  backend-base-url: http://localhost:8080
  frontend-base-url: http://localhost:5173
  success-url: ${fiqo.frontend-base-url}/login

storage:
  strategy: ${STORAGE_STRATEGY:s3}
  s3:
    base-url: ${MINIO_BASE_URL:http://localhost:9000}
    bucket: ${MINIO_BUCKET_NAME:fiqo}
    credentials:
      access-key: ${MINIO_ACCESS_KEY:admin}
      secret-key: ${MINIO_SECRET_KEY:admin123}

jwt:
  access-exp-min: 5
  refresh-exp-min: 30
  access-key: 561744090F30C5564F793305AB783BC96545E88EE42A7ED0CED578C72CE132A3
  refresh-key: 42DCCF1E0B06EF601CF9DCDA0ED3877F4F0DB32FAE243DC5F0C681DB09AFB454

spring:
  datasource:
    url: ${fiqo-db.url}
    driver-class-name: org.postgresql.Driver
    username: ${fiqo-db.username}
    password: ${fiqo-db.password}

  jpa:
    show-sql: false
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
      format_sql: true
    database: postgresql

  flyway:
    enabled: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:YOUR_GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET:YOUR_GOOGLE_CLIENT_SECRET}
            scope:
              - openid
              - profile
              - email
            redirect-uri: ${fiqo.backend-base-url}/login/oauth2/code/google
            redirect-uri-login: ${fiqo.frontend-base-url}/google-callback-login.html
            redirect-uri-connect: ${fiqo.frontend-base-url}/google-callback-connect.html
            authorization-grant-type: authorization_code
          github:
            client-id: ${GITHUB_CLIENT_ID:YOUR_GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET:YOUR_GITHUB_CLIENT_SECRET}
            scope:
              - read:user
              - user:email
            redirect-uri: ${fiqo.backend-base-url}/login/oauth2/code/github
            redirect-uri-login: ${fiqo.frontend-base-url}/github-callback-login.html
            redirect-uri-connect: ${fiqo.frontend-base-url}/github-callback-connect.html
            authorization-grant-type: authorization_code
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://openidconnect.googleapis.com/v1/userinfo
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user
            email-uri: https://api.github.com/user/emails

  mail:
    host: YOUR_SMTP_HOST
    port: 587
    username: YOUR_SENDER_EMAIL
    password: YOUR_SENDER_EMAIL_PASSWORD
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
